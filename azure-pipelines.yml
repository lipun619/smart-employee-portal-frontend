# ============================================================
# Frontend CI/CD Pipeline — Smart Employee Portal Angular 20
# ============================================================
# Triggers on every push to main branch (angular folder changes only)
# Steps: npm ci → ng build → deploy to Azure Static Web App

trigger:
  branches:
    include:
      - master
      - main
  paths:
    include:
      - src/**
      - angular.json
      - package.json
      - package-lock.json
      - azure-pipelines.yml

pool:
  vmImage: 'ubuntu-latest'

variables:
  nodeVersion: '22.x'
  angularProject: '.'

stages:

  # ── Stage 1: Build Angular ───────────────────────────────────────────────
  - stage: Build
    displayName: 'Build Angular App'
    jobs:
      - job: BuildAngular
        displayName: 'npm ci + ng build'
        steps:

          - task: NodeTool@0
            displayName: 'Install Node.js $(nodeVersion)'
            inputs:
              versionSpec: $(nodeVersion)

          - script: |
              npm cache clean --force
              npm install --no-package-lock --registry=https://registry.npmjs.org/
            displayName: 'Install dependencies from npmjs registry'
            workingDirectory: $(angularProject)

          - script: npx ng build --configuration production
            displayName: 'Build Angular (production)'
            workingDirectory: $(angularProject)

          - task: PublishBuildArtifacts@1
            displayName: 'Upload Angular artifact'
            inputs:
              pathToPublish: 'dist/smart-employee-portal/browser'
              artifactName: 'SmartEmployeePortal-Angular'

  # ── Stage 2: Deploy to Azure Static Web App ──────────────────────────────
  - stage: Deploy
    displayName: 'Deploy to Azure Static Web App'
    dependsOn: Build
    condition: and(succeeded(), or(eq(variables['Build.SourceBranch'], 'refs/heads/main'), eq(variables['Build.SourceBranch'], 'refs/heads/master')))
    jobs:
      - deployment: DeployAngular
        displayName: 'Deploy Angular to swa-sep-dev'
        environment: 'Development'
        strategy:
          runOnce:
            deploy:
              steps:
                - task: DownloadBuildArtifacts@1
                  displayName: 'Download Angular artifact'
                  inputs:
                    buildType: 'current'
                    downloadType: 'single'
                    artifactName: 'SmartEmployeePortal-Angular'
                    downloadPath: '$(System.DefaultWorkingDirectory)'

                - task: AzureStaticWebApp@0
                  displayName: 'Deploy to Static Web App'
                  inputs:
                    app_location: 'SmartEmployeePortal-Angular'
                    output_location: ''
                    skip_app_build: true
                    azure_static_web_apps_api_token: '$(SWA_DEPLOYMENT_TOKEN)'
